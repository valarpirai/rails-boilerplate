<div class="row">
    <div class="col">
    </div>
    <div class="col">

    <h2>Account Signup</h2>

    <%= form_with url: accounts_signup_path, method: 'post' do |f| %>

        <div class="form-group">
            <div class="form-row">
                <div class="col-md-6">
                    <%= f.text_field 'signup[first_name]', autofocus: true, class: "form-control", placeholder: "First Name", required: "true" %>
                </div>
                <div class="col-md-6">
                    <%= f.text_field 'signup[last_name]', autofocus: true, class: "form-control", placeholder: "Last Name", required: "true" %>
                </div>
            </div>
        </div>

        <div class="form-group">
            <%= f.email_field 'signup[email]', autocomplete: "email", class: "form-control", placeholder: "Business Email", required: "true" %>
        </div>

        <div class="form-group">
            <%= f.text_field 'signup[company]', autocomplete: "company", class: "form-control", placeholder: "Company", required: "true" %>
        </div>
        

        <div class="form-group">
            <%= f.submit "Signup", class: "btn btn-primary" %>
        </div>

    <% end %>
    </div>
</div>